global:
  scrape_interval: 30s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: onsite-backend
    metrics_path: /metrics
    static_configs:
      - targets: ['api:3000']
